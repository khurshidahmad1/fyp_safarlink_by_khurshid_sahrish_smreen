import 'package:flutter/material.dart';

class TokenCheckbox extends StatelessWidget {
  const TokenCheckbox({super.key});

  @override
  Widget build(BuildContext context) {
    return Checkbox(value: false, onChanged: (v){});
  }
}
