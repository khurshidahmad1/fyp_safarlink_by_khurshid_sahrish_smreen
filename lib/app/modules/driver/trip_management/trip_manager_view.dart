import 'package:flutter/material.dart';

class TripManagerView extends StatelessWidget {
  const TripManagerView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(body: Center(child: Text('Trip Management View')));
  }
}
