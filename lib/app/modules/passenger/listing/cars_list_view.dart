import 'package:flutter/material.dart';

class CarsListView extends StatelessWidget {
  const CarsListView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(body: Center(child: Text('Cars List View')));
  }
}
