import 'package:flutter/material.dart';

class PassengerTripsView extends StatelessWidget {
  const PassengerTripsView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(body: Center(child: Text('My Trips View')));
  }
}
